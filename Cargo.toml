[package]
name = "anthropic"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/mariogeiger/anthropic"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
